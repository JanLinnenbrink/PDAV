% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twcv.R
\name{compute_twcv_buffered}
\alias{compute_twcv_buffered}
\title{Compute TWCV-weighted estimators from buffered CV tasks}
\usage{
compute_twcv_buffered(
  sample_dat,
  grid_dat,
  task_list,
  response = "z",
  predictors,
  fit_fun,
  predict_fun,
  model = NULL,
  twcv_specs = NULL,
  env_vars = predictors,
  loss_engine = c("auto", "taskwise", "reuse", "fold"),
  ...
)
}
\arguments{
\item{sample_dat}{Data frame with sampled observations.}

\item{grid_dat}{Data frame with deployment locations.}

\item{task_list}{List of buffered CV tasks. Each element must contain
\code{test_rows} and \code{train_rows}.}

\item{response}{Response column name.}

\item{predictors}{Predictor variable names used for model fitting.}

\item{fit_fun}{Model fitting function.}

\item{predict_fun}{Prediction function.}

\item{model}{Optional model label passed to \code{fit_fun}.}

\item{twcv_specs}{Named list of TWCV specifications. If \code{NULL}, a default
set is constructed from \code{predictors}.}

\item{env_vars}{Optional task-descriptor variables. Defaults to \code{predictors}.}

\item{loss_engine}{Buffered loss engine. \code{"taskwise"} fits one model per
task; \code{"reuse"} fits one model per unique training configuration; \code{"auto"}
chooses between them. \code{"fold"} is accepted as an alias for \code{"reuse"}.}

\item{...}{Additional arguments passed to \code{fit_fun}.}
}
\value{
List with losses, estimators, weights, specs, and chosen engine.
}
\description{
Uses externally supplied buffered validation tasks, computes pointwise losses,
realized prediction distances, and returns unweighted and TWCV-weighted
performance summaries.
}
\details{
Each task must provide at least \code{test_rows} and \code{train_rows}. Optionally it
may also contain \code{task_id}.
}
