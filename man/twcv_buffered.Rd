% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twcv.R
\name{twcv_buffered}
\alias{twcv_buffered}
\title{Estimate deployment-oriented predictive performance from buffered tasks (Not used/exported at the momement)}
\usage{
twcv_buffered(
  sample_dat,
  grid_dat,
  task_obj,
  model = c("rf", "lm", "ked_het_x1", "ked", "ked_het_pop"),
  response = NULL,
  fit_fun = fit_model,
  predict_fun = predict_model,
  verbose = 0,
  twcv_specs = NULL,
  predictor_vars = NULL,
  env_vars = NULL,
  iwcv_vars = NULL,
  run_iwcv = FALSE,
  iwcv_shrink_lambda = 0,
  ...
)
}
\arguments{
\item{sample_dat}{Data frame of sampled observations used for validation.}

\item{grid_dat}{Data frame representing deployment or prediction locations.}

\item{task_obj}{Buffered task object, typically created by
generate_buffered_loo_tasks().}

\item{model}{Character string identifying the prediction model.}

\item{response}{Optional response variable name. If \code{NULL}, the function
tries \code{z} and then \code{outcome}.}

\item{fit_fun}{Model-fitting function passed to
compute_buffered_task_losses(). It must accept at least \code{train_dat},
\code{model}, and \code{response}.}

\item{predict_fun}{Prediction function passed to
compute_buffered_task_losses(). It must accept a fitted model object and
\code{newdata}.}

\item{verbose}{Verbosity level.}

\item{twcv_specs}{Optional named list of TWCV specifications.}

\item{predictor_vars}{Optional character vector of predictor variables used
by the predictive model.}

\item{env_vars}{Optional character vector of environmental variables used as
task descriptors. Defaults to \code{predictor_vars}.}

\item{iwcv_vars}{Optional character vector of variables for IWCV density-ratio
estimation. Prediction distance \code{d} is appended internally when IWCV is
enabled.}

\item{run_iwcv}{Logical; if \code{TRUE}, also compute IWCV.}

\item{iwcv_shrink_lambda}{Shrinkage parameter for IWCV weights.}

\item{...}{Additional arguments passed to \code{fit_fun}.}
}
\value{
A list with elements:
\describe{
\item{losses}{Buffered validation-loss data frame augmented with task
descriptors.}
\item{sample_tasks_bal}{Currently \code{NULL}; retained for backward
compatibility.}
\item{grid_tasks_bal}{Currently \code{NULL}; retained for backward
compatibility.}
\item{estimators}{Named list of performance summaries.}
\item{weights}{Named list of weight objects for DWCV, TWCV, and
optionally IWCV.}
\item{twcv_specs}{The TWCV specification set actually used.}
}
}
\description{
Analogous to \code{\link[=twcv]{twcv()}}, but based on externally generated
buffered validation tasks, typically from buffered leave-one-out resampling.
Validation losses are computed for the selected buffered tasks, augmented
with realized task descriptors, and summarized using unweighted, DWCV, TWCV,
and optionally IWCV estimators.
}
\details{
Model fitting and prediction are supplied explicitly through \code{fit_fun} and
\code{predict_fun}, allowing the shared buffered-task estimator engine to be
reused across case studies without redefining global adapter functions.
}
\seealso{
\code{\link[=twcv]{twcv()}}, \code{\link[=generate_buffered_loo_tasks]{generate_buffered_loo_tasks()}}
}
