% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caret_cv_estimators.R
\name{compute_cv_estimators_caret}
\alias{compute_cv_estimators_caret}
\title{Compute environmental similarity weighted CV metric from caret results}
\usage{
compute_cv_estimators_caret(
  train_obj,
  predictors,
  samples,
  response = NULL,
  env_vars = NULL,
  metric_fun = rmse_metric,
  balance_by = 0.2,
  shrink_lambda = 0.2,
  verbose = 0,
  ...
)
}
\arguments{
\item{train_obj}{A \code{caret::train} object created with
\code{trainControl(savePredictions = "all")}.}

\item{predictors}{A \code{terra::SpatRaster} containing environmental predictor
layers for the prediction domain.}

\item{samples}{An \code{sf} object containing the training locations and the
original response values used to fit \code{train_obj}.}

\item{response}{Optional response column name. If \code{NULL}, the response is
inferred from \code{train_obj} or from \code{samples}.}

\item{env_vars}{Optional character vector of raster layer names to use for
environmental similarity. If \code{NULL}, all layers in \code{predictors} are used.}

\item{metric_fun}{Function with signature \verb{function(obs, pred, weights = NULL)}
that returns a scalar performance metric. The default is RMSE.}

\item{balance_by}{Numeric quantile bin width used for discretizing environmental
variables during weighting.}

\item{shrink_lambda}{Numeric shrinkage parameter for calibration weights.}

\item{verbose}{Integer verbosity level.}

\item{...}{Additional arguments passed to \code{terra::extract()} when extracting
raster values for the sample locations.}
}
\value{
A list with components:
\itemize{
\item \code{unweighted_metric}: the CV metric computed without weighting.
\item \code{weighted_metric}: the CV metric after environmental reweighting.
\item \code{weights}: the normalized calibration weights used for reweighting.
\item \code{losses}: data frame of out-of-fold observations, predictions, and metrics.
\item \code{grid_tasks}: environmental descriptors for the prediction grid.
}
}
\description{
Compute a deployment-oriented CV metric by reweighting caret out-of-fold
predictions to match the environmental distribution of a raster prediction grid.
}
\details{
This is a simplified variant of the package's TWCV workflow that ignores
explicit spatial distance and uses only environmental predictor space.
}
