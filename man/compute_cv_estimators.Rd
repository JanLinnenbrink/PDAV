% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twcv.R
\name{compute_cv_estimators}
\alias{compute_cv_estimators}
\title{Estimate deployment-oriented predictive performance from CV tasks}
\usage{
compute_cv_estimators(
  sample_dat,
  grid_dat,
  folds,
  model = c("rf", "lm", "ked_het_x1", "ked", "ked_het_pop"),
  response = NULL,
  fit_fun = fit_model,
  predict_fun = predict_model,
  verbose = 0,
  twcv_specs = NULL,
  predictor_vars = NULL,
  env_vars = NULL,
  ...
)
}
\arguments{
\item{sample_dat}{Data frame of sampled observations used for validation.
Must contain an ID column and the response variable.}

\item{grid_dat}{Data frame representing deployment or prediction locations.}

\item{folds}{Integer vector of fold assignments for \code{sample_dat}.}

\item{model}{Character string identifying the prediction model.}

\item{response}{Optional response variable name. If \code{NULL}, the function
tries \code{z} and then \code{outcome}.}

\item{fit_fun}{Model-fitting function passed to compute_cv_losses(). It
must accept at least \code{train_dat}, \code{model}, and \code{response}.}

\item{predict_fun}{Prediction function passed to compute_cv_losses(). It
must accept a fitted model object and \code{newdata}.}

\item{verbose}{Verbosity level.}

\item{twcv_specs}{Optional named list of TWCV specifications. Each
specification must contain \code{balancing_vars}, \code{balance_by}, and
\code{shrink_lambda}. If \code{NULL}, a default extended TWCV specification is used.}

\item{predictor_vars}{Optional character vector of predictor variables used
by the predictive model. If \code{NULL}, common non-coordinate, non-response
variables are inferred from \code{sample_dat} and \code{grid_dat}.}

\item{env_vars}{Optional character vector of environmental variables used as
task descriptors. Defaults to \code{predictor_vars}.}

\item{...}{Additional arguments passed to \code{fit_fun} during validation.}
}
\value{
A list with elements:
\describe{
\item{losses}{Validation-loss data frame augmented with task descriptors.}
\item{estimators}{Named list of performance summaries for the unweighted TWCV.}
\item{weights}{Named list of weight object for TWCV.}
\item{twcv_specs}{The TWCV specification set actually used.}
}
}
\description{
Computes validation losses for a given cross-validation design, augments them
with realized task descriptors, and summarizes predictive performance under
several estimators targeting different task distributions. In addition to the
unweighted estimator, the function computes distance-weighted CV (DWCV),
target-weighted CV (TWCV), and optionally importance-weighted CV (IWCV).
}
\details{
Model fitting and prediction are supplied explicitly through \code{fit_fun} and
\code{predict_fun}, allowing the shared estimator engine to be reused across case
studies without redefining global adapter functions.
}
\examples{
\dontrun{
# Example data:
# sample_dat contains observed responses at sampled locations,
# grid_dat contains the deployment locations where predictions are intended.

set.seed(1)

n_sample <- 120
n_grid <- 500

sample_dat <- data.frame(
  id = seq_len(n_sample),
  x = runif(n_sample),
  y = runif(n_sample),
  x1 = rnorm(n_sample),
  x2 = runif(n_sample),
  z = NA_real_
)

sample_dat$z <- 1 + 2 * sample_dat$x1 - sample_dat$x2 + rnorm(n_sample, sd = 0.3)

grid_dat <- data.frame(
  id = seq_len(n_grid),
  x = runif(n_grid),
  y = runif(n_grid),
  x1 = rnorm(n_grid),
  x2 = runif(n_grid)
)

# Five-fold CV assignment
folds <- sample(rep(1:5, length.out = n_sample))

# Simple model adapters used by compute_cv_estimators()
fit_lm <- function(train_dat, model, response, ...) {
  stats::lm(stats::as.formula(
    paste(response, "~", paste(c("x1", "x2"), collapse = " + "))
  ), data = train_dat)
}

predict_lm <- function(object, newdata) {
  stats::predict(object, newdata = newdata)
}

res <- compute_cv_estimators(
  sample_dat = sample_dat,
  grid_dat = grid_dat,
  folds = folds,
  model = "lm",
  response = "z",
  fit_fun = fit_lm,
  predict_fun = predict_lm,
  predictor_vars = c("x1", "x2"),
  env_vars = c("x1", "x2"),
  verbose = 1
)

plot(res)

# Augmented validation losses
head(res$losses)

# Performance summaries
res$estimators

# TWCV weight object(s)
names(res$weights)
}
}
\seealso{
compute_buffered_estimators(), compute_twcv_weights()]
}
